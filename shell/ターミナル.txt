ターミナル

ターミナルとは：
決められたコマンドで作業をするツール

ターミナルを使う理由：
単純作業に強い。
コマンドでしか使えないツールがある。

ターミナルの欠点：
コマンドを知らないと何もできない。

ターミナルの使い方
起動方法：Show application - Terminal
コマンド [オプション] (return)
(return) ＝ (return=enter)キーを押す