#! /usr/bin/bash

mkdir run-in-background-test & cd run-in-background-test
echo "mkdir command runned successfully but cd command denied."
echo "because cd command and mkdir command runned at the same time."
